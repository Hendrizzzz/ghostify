<!-- For scope questions, see CONTRIBUTING.md. Squash-merge only; keep the title in Conventional Commits format (fix:, feat:, chore(status):, ...). -->

## What changed

<!-- Files/areas changed and why. Link related issues. -->

## Commands run

<!-- e.g. npm run build, npm test, npm run ci — include the actual results. -->

- [ ] `dist/` bundles rebuilt and committed together with `src/` changes (required for any `src/` edit)
- [ ] `npm run ci` passes locally

## Smoke status

Use the evidence vocabulary from CONTRIBUTING.md / docs/QA_FIXTURES.md.

- Surfaces touched:
- Status: `verified` / `manual-pending` / `gap` / `not applicable` (per smoke ID)

## Known risks

<!-- What could regress? Which pinned tests or docs did you review/update? -->
